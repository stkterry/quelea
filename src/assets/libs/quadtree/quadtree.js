
class QuadTree {
  counstructor(bounds) {
    this.bounds = bounds;
    this.cap = n;
    this.points = [];
  }

  insert(points) {
    if (this.points.length < this.capacity) this.points.push(point)
    else this.subdivide();
  } 

}
export default QuadTree;